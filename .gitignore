# =============================================================================
# IIOS — .gitignore
# =============================================================================

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!web_dashboard/frontend/src/lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# ── .env files (SECRETS — never commit real credentials) ─────────────────────
.env
.env.*
# Always commit the example template
!.env.example

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo
*~

# ── Type checking ─────────────────────────────────────────────────────────────
.mypy_cache/
.dmypy.json
dmypy.json

# ── Testing ───────────────────────────────────────────────────────────────────
.pytest_cache/
.hypothesis/

# ── Coverage ──────────────────────────────────────────────────────────────────
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# ── Logs (runtime generated — not source) ────────────────────────────────────
logs/
*.log
*.log.*

# ── IIOS Runtime data ─────────────────────────────────────────────────────────
# Broker session tokens (Kite access token etc.) — secrets, never commit
data/broker/

# data/ contains a mix of static reference data (tracked) and live runtime
# state (must NOT be tracked). Persistence across deploys/restarts comes
# from the Docker bind mount (./data:/app/data in docker-compose.yml) —
# NOT from git tracking. Tracking live runtime files is actively DANGEROUS:
# any future `git pull` on the VPS silently reverts them to whatever was
# last committed, discarding all live data with no warning. This caused a
# real production incident on 2026-09-04 (control_tower.db reverted to a
# months-old commit mid-deploy, wiping out same-day repairs) — see
# /memories/repo/incident_control_tower_db_git_tracked.md for the full
# post-mortem. The rule going forward: runtime databases/logs/CSVs are
# ALWAYS gitignored; only static config/reference JSON is ever tracked.
data/reports/
data/backups/
data/*.bak
data/.bootstrap_probe
data/.hc_probe
data/replay.db
data/*.db
data/*.db-wal
data/*.db-shm
data/*.db-journal
data/mls/*.db
data/ikn/*.db
data/paper_trades*.csv
data/paper_trade_log.csv
data/live/*.jsonl

# ── Large runtime-grown knowledge stores (written by momentum_knowledge_builder)
# These grow to 30-100MB on VPS and must NOT be overwritten by git pull.
# The seed content (2,356 records) is committed once; the VPS version is live.
data/knowledge_evidence_ledger.jsonl
data/shadow_evidence_ledger.jsonl

# ── Runtime state files — updated every cycle on VPS; wiping resets live state ──
# fresh-deploy is graceful (system rebuilds empty state automatically).
data/ksl/*.json
data/ksl/*.jsonl
data/kda_cre/
data/research_question_queue.jsonl
data/regime_probability_history.json
data/scanner_memory.json
data/iv_history.json
data/lol/LOL_*.jsonl
data/eod_status.json
data/kda_outcome_backfill_state.json
data/klp/
data/execution_conversion/

# ── Options runtime data — generated fresh each session ──────────────────────
data/options_*.json
data/options_*.jsonl
data/options_*.csv

# ── Other runtime state files ─────────────────────────────────────────────────
data/scheduler_health.json
data/trade_analytics_*.json

# ── Strategy / learning runtime state — CRITICAL: never reset these ──────────
# git pull must NEVER overwrite these; they are the system's earned knowledge.
# NOTE: each pattern MUST be on its own line with no trailing inline comment —
# gitignore treats "pattern  # comment" as one literal pattern (never matches).
# win rates per strategy — rebuilt every EOD
data/strategy_performance.json
# OpportunityDensityMonitor signal tier history
data/odm_state.json
# KDA pipeline health metrics
data/knowledge_pipeline_health.json
# MetaLearning k-NN training records (21+ obs)
data/ml_performance_dataset.json
# daily P&L summary
data/paper_trading_daily.json

# ── Research outputs generated on VPS — months of accumulated work ────────────
# 600KB+ discovered trading edges
data/discovered_edges.json
# evolved strategy index cache
data/evolved_strategies.json
# ARS hypothesis registry
data/ars_hypothesis_registry.json
# ARS study outputs
data/ars_study_*.json
# ARS research coordinator history
data/ars/rc/history.json
# ARS scheduler run history (Priority 1 activation)
data/ars/scheduler/
data/ars/sfr/
data/ars/sd/
data/ars_roadmap_state.json
data/mover_discovery_v3_shadow.jsonl
data/mover_discovery_v3_shadow_raw.jsonl
# Debate weight self-tuning (Priority 3 activation)
data/debate/
# Decision tracer scheduler (Priority 4 activation)
data/dta/
# Regime map evidence log + refinement engine (Priority 5 activation)
data/meta_learning/
# OIOS ELE age-advance cadence guard (root-cause fix: DISCOVERED-stuck bug)
data/oios/
# StrategyHealthMonitor regime-aware disabling (self-learning module #24)
data/trade_monitoring/
# Trust-weighted candidate ranking (self-learning module #26)
data/data_integrity/
# Sizing bounds calibration (self-learning module #27)
data/risk_control/
# Options health metrics dashboard (item #10)
data/data_feeds/
# Dynamic target expansion evidence + calibration (self-learning module #29)
data/learning_system/

# ── Per-session monitoring / rebuild runtime state ────────────────────────────
data/monitor_state.json
data/nifty500_universe.json
data/closed_orders_*.txt
data/expiry_retries.json
data/borderline_rejections.json
data/stability_ledger.json
data/rejection_audit.db

# ── KDA daily comparison + scan attrition (written every intraday cycle) ──────
# Retained for 30 days then summarised by data_cleanup.py — raw files not needed
data/klp/kda/
data/predictive_gap/
data/klp/knowledge_fusion/source_inventory.jsonl
data/klp/knowledge_fusion/source_inventory.jsonl

# ── DTA-MARKET-BENCHMARK-001 — daily benchmark outputs (written every EOD) ────
data/market_benchmark/

# ── Additional runtime/research directories confirmed untracked-and-unignored
# during the 2026-09-14 housekeeping audit — same category as everything
# above (generated fresh every cycle, never hand-authored, safe to ignore).
data/klp/historical_behaviour/
data/audit/dta038/
data/audit/dta041/
data/cle/
data/early_move_audit/
data/exit_analytics/
data/ikn/
data/ilc/
data/learning/
data/mop_rc001/
data/gva/
data/pga/
data/pga_learning_actions.json
data/prr/
data/real_options/
data/alert_governance_state.json
data/champion_challenger_registry.jsonl
data/controlled_live_candidates.json
data/eod_retro_*.txt
data/fingerprint_discovery_daily.jsonl
data/live_selection_eligibility.json
data/live_selection_eligibility_audit.jsonl
data/regime_strategy_map.json
data/risk_guardian_state.json
data/rejection_attribution/
data/hkap/
data/kde/
data/sandy/

# ── Docker ────────────────────────────────────────────────────────────────────
.docker-data/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Notebooks (analysis only, not production) ─────────────────────────────────
*.ipynb
.ipynb_checkpoints/

# ── Temporary analysis scripts ────────────────────────────────────────────────
analysis_temp.py
*_temp.py

# ── Benchmarks ────────────────────────────────────────────────────────────────
.benchmarks/

# ── Secrets baseline (DO commit this file once generated) ─────────────────────
# .secrets.baseline

# ── Broker token files (contain live JWTs — never commit) ─────────────────────
config/api_tokens.json