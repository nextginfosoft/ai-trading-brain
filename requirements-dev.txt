# ── Install production dependencies first ─────────────────────────────────────
-r requirements.txt

# =============================================================================
# Testing
# =============================================================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
httpx2                        # starlette TestClient backend (web_dashboard tests)
reportlab>=4.0                # builds docs/TradeSense_AI_Workflow.pdf (docs/build_workflow_pdf.py)
pytest-benchmark>=4.0.0
pytest-timeout>=2.3.0
hypothesis>=6.100.0
freezegun>=1.4.0         # Freeze time for deterministic tests
responses>=0.25.0        # Mock HTTP requests
time-machine>=2.14.0     # Fast time manipulation

# =============================================================================
# Code Quality — Formatting
# =============================================================================
black>=24.3.0
isort>=5.13.0

# =============================================================================
# Code Quality — Linting
# =============================================================================
ruff>=0.4.0
flake8>=7.0.0
flake8-bugbear>=24.2.0
flake8-annotations>=3.0.0

# =============================================================================
# Type Checking
# =============================================================================
mypy>=1.9.0
pandas-stubs>=2.1.0
types-requests>=2.31.0
types-pytz>=2024.1.0
types-python-dateutil>=2.9.0
types-tqdm>=4.66.0

# =============================================================================
# Security
# =============================================================================
detect-secrets>=1.4.0    # Pre-commit secrets scanner
bandit[toml]>=1.7.0      # Static security analysis
pip-audit>=2.7.0         # CVE scanning
safety>=3.1.0            # Dependency vulnerability check

# =============================================================================
# Pre-commit
# =============================================================================
pre-commit>=3.7.0

# =============================================================================
# Developer Utilities
# =============================================================================
ipython>=8.22.0          # Enhanced REPL for debugging
rich>=13.7.0             # Rich terminal output
watchdog>=4.0.0          # File watching for dev auto-reload

# =============================================================================
# VS Code chat/MCP surface (Sandy — local dev tool only, never in the
# production Docker image; see sandy/sandy_mcp_server.py)
# =============================================================================
mcp>=1.0.0
